# =============================================================================
# Hermes Alpha Hunter — Fly.io Configuration
# Persistent Telegram gateway for autonomous security analysis
# =============================================================================

app = "hermes-alpha-hunter"
primary_region = "sin"

[build]
  dockerfile = "Dockerfile"

[[vm]]
  size = "shared-cpu-2x"
  memory = 2048

[env]
  PYTHONUNBUFFERED = "1"
  HERMES_HOME = "/root/.hermes"
  HERMES_YOLO_MODE = "1"

[[restart]]
  policy = "always"
  retries = 10
